apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dualoj-judge-api.fullname" . }}-test-connection"
  labels:
    {{- include "dualoj-judge-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dualoj-judge-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
